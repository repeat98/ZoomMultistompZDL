# Airwindows Exact-Port Policy

Canonical policy now lives in
[../../docs/AIRWINDOWS-EXACT-PORTS.md](../../docs/AIRWINDOWS-EXACT-PORTS.md).

This file is kept only as a local pointer for older notes that linked to
`src/airwindows/AIRWINDOWS-EXACT-PORTS.md`. Do not maintain a second copy of
the policy here; it drifted behind the hardware findings once `StereoChorus`
proved the `ctx[3]` arena and `ToTape9` exposed the next load-time boundary.
